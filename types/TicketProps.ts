type TicketProps = {
  number_of_places: number;
  time_of_session: string;
  total_price: number;
  qrcodeImage: string;
  name: string;
  description?: string;
  date_of_session: string;
};

export default TicketProps;
